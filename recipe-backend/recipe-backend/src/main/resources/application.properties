spring.application.name=recipe-backend
# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/food-recipe
spring.data.mongodb.auto-index-creation=true

# JWT Configuration
jwt.secret=zxcvbnmasdfghjklqwertyuiop
jwt.expiration=86400000

# Server Configuration
server.port=8080

# Session Configuration
server.servlet.session.timeout=30m

# Logging Configuration
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.org.springframework.web=DEBUG

# Jackson Configuration for JSON
spring.jackson.default-property-inclusion=NON_NULL
spring.jackson.serialization.write-dates-as-timestamps=false

# Validation Configuration
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=uploads
app.upload.images.path=./uploads/images/
app.upload.videos.path=./uploads/video/

# Firebase Configuration
firebase.service-account-key-path=firebase-service-account.json

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ducpham10111995@gmail.com
spring.mail.password=jeua zzjx ycbe vbek
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
